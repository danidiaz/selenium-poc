La aplicación espera tener los ejecutables "chrome" y "chromedriver" en el PATH.

Para compilar:

    mvn install
    mvn spring-boot:run

Para obtener la captura:

    curl -o capture.png localhost:8080/capture

